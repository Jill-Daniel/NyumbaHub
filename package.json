{
  "name": "nyumbahub",
  "version": "1.0.0",
  "description": "Meru University student housing platform",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "install:vercel": "npm install && npm run install:all",
    "seed": "npm run seed --prefix server",
    "build:vercel": "npm run seed && npm run build --prefix client",
    "dev": "npm run seed && concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run seed && concurrently \"npm start --prefix server\" \"npm run preview --prefix client\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "serverless-http": "^3.2.0",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
